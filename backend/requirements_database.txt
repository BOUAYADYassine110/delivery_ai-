# Additional Requirements for Database Integration

# MongoDB async driver
motor==3.3.2

# MongoDB sync driver (for compatibility)
pymongo==4.6.1

# Environment variables
python-dotenv==1.0.0

# Data validation (already in requirements but ensure version)
pydantic>=2.0.0

# Async support
asyncio>=3.4.3
